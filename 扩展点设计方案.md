# Jellyfish 扩展点设计方案

> 本文定义 Jellyfish 插件体系的**语义层**（形状预设）与**实例层**（扩展点、回调类型、事件类型的契约）。
> 机制层（回调通道 / 事件通道）见 `双通道改造方案.md`。
>
> 本文替代并合并原 `扩展点事件类型清单.md`。
>
> 关联文档：`AGENTS.md`、`Jellyfish技术方案.md`、`跨语言插件方案.md`、`双通道改造方案.md`

---

## 一、定位与总纲

### 1.1 三层分层

```
机制层   回调通道 / 事件通道                     ← 只区分「有无返回值」
语义层   A 贡献 / B 提供 / C 观察 / D 拦截 / E 策略 ← 每个形状一组参数预设
实例层   扩展点 · 回调类型 · 事件类型              ← 本文档 §四 / §六 / §七
```

计划插件的人只看实例层；形状的参数是内核的实现细节。

### 1.2 核心主张

> **内核只定形状，SKILLS / MCP / Memory / TODO 全部做成内核之外的官方插件。**

这既是 "Keep the Kernel Tiny" 的落地，也是一次架构自检：

> **如果 MCP 用现有扩展点拼不出来，说明缺的是扩展点（例如动态工具注册），而不是「MCP 需要特殊支持」。**

### 1.3 扩展点的定义与边界

> **扩展点 = 内核调用插件的点。**（方向：内核 → 插件）

由此推出边界：

| 项 | 方向 | 归类 |
| --- | --- | --- |
| 内核回调插件（注册处理器、订阅通知） | 内核 → 插件 | **扩展点** |
| 插件发通知（`publisher()`） | 插件 → 内核 | **能力**（不是扩展点） |
| 插件读取自己的配置 | 插件 → 内核 | **能力** |
| 插件查询已注册工具 | 插件 → 内核 | **能力** |
| 插件主动投递消息（`session.trigger()`） | 插件 → 内核 | **能力** |

**判据：扩展点的数量 = 内核主动回头找插件的次数。这个数字应当很小，且不随功能增长。**

### 1.4 SPI 的组织方式

- **插件（Plugin）= 部署单元**：一个 jar、一个 `plugin.properties`、一份权限声明、一个生命周期 → **只有一个 `JellyfishPlugin`**。
- **扩展点（Extension Point）= 能力单元**：一个形状 + 一份确定的入参/出参契约 → **有多个**。

**不做按功能切分的 SPI**（`ToolPlugin` / `MemoryPlugin` / `CrossPlugin`）：功能是发散的，每加一类能力就要改 `jellyfish-api`，与 §1.2 直接矛盾。

**插件"是什么插件"由它注册了哪些扩展点决定（capability = 你注册了什么），不由继承体系决定。**

**A / B / D / E 一律用注册式实现，不用接口实现。** 理由：

1. 脚本插件无法实现 Java 接口，接口实现会破坏跨语言能力同构；
2. 回调通道泛化后，"注册一个处理器"是唯一的统一语义（见 `双通道改造方案.md`）；
3. 注册式天然携带 `order`、owner、扩展点归属等元信息，接口实现表达不了。

---

## 二、已确认的设计结论

| # | 结论 |
| --- | --- |
| 1 | **热部署一致性**：贡献类（A）在**组装时快照**，热部署只对下一次组装生效 |
| 2 | **系统提示词与会话绑定**：系统提示词在**会话创建 / 会话内切换 Agent 时**组装并快照，会话内不再重组；`prompt.context.contribute` 才每轮组装 |
| 3 | **TODO 用 A + B 实现**，不为此开循环钩子 |
| 4 | **PLAN 模式不作为拦截实现**：先由 `PermissionManager` 判定放过，**再**询问插件是否 deny（单调 AND 链）。插件**只能 deny / escalate，永远不能 grant** |
| 5 | **失败语义**：策略类（E）**fail-closed**（异常/超时即视为 deny）；其余形状 **fail-open**（丢弃该贡献并降级），不阻塞主流程 |
| 6 | **`requiresApproval` 与 PLAN 模式的关系**：前者是持久化策略配置，后者是会话级临时开关，二者**汇入同一个「暂停并询问」的落点**，不演变成两套审批路径 |
| 7 | **补充工具元数据模型**：`tool.provide` 需声明 description、参数 JSON Schema、tags、**是否有副作用 / 是否幂等**。api 层需要 `ToolDescriptor`，由内核转成 `LlmTool` |
| 8 | **补充插件配置注入**：`jellyfish.json` 需要插件配置段，经 `PluginContext` 暴露；不允许插件自行读文件 |
| 9 | **补齐能力声明 / 可查询 / 可降级**：每个扩展点有稳定 ID，在 `plugin.properties` 声明，由 `PermissionManager` 校验；插件崩溃时降级为「工具不可用」而非「进程崩」 |
| 10 | **越权在加载期暴露**：冲突与越权在插件加载期报错，不在首次调用时 |
| 11 | **TUI / CLI 呈现渠道**：后续再实现，当前不考虑 |
| 12 | **`order` 只对有序形状开放**（A / D / E）；B 按 key 路由，无先后语义 |
| 13 | **A / B / D / E 用注册式，不用接口实现**（跨语言能力同构的硬约束） |
| 14 | **形状给默认参数，扩展点可覆盖**；覆盖必须显式声明在扩展点定义里 |
| 15 | **除明确不做的项以外，全部实现**（见 §十） |
| 16 | **持久化粒度：每条消息同步落盘**。不接受「进程崩溃丢最后半轮」；代价是主流程承担一次同步写入 |

---

## 三、五种扩展形态

| 形状 | 语义 | 通道 | 参数预设（可被扩展点覆盖） |
| --- | --- | --- | --- |
| **A 贡献** | 多→一，同步，内核负责排序与合并 | 回调通道 | `unique=false, ordered=true, OPTIONAL, MANY, ISOLATED` |
| **B 提供** | 多→一，按 key 路由，恰好一个 | 回调通道 | `unique=true, ordered=false, REQUIRED, ONE, INLINE` |
| **C 观察** | 多→多，异步，无返回值 | 事件通道 | 广播语义 |
| **D 拦截** | 有序链，同步，可改写 / 可短路 | 回调通道 | `unique=false, ordered=true, OPTIONAL, MANY, ISOLATED` |
| **E 策略** | 只能「收紧」，单向 | 回调通道 | `unique=false, ordered=true, OPTIONAL, MANY, ISOLATED` |

**组合规则由调用方决定，不是通道的属性：**

| 形状 | 组合规则 |
| --- | --- |
| A 贡献 | 聚合（收集片段，按剩余预算裁剪后合并） |
| D 拦截 | 链式（后者拿到前者的输出） |
| E 策略 | AND（任一 deny 即拒） |

---

## 四、扩展点清单（定稿）

### 4.1 扩展点

| # | 扩展点 ID | 形状 | **通道** | 职责 | 权限常量 |
| --- | --- | --- | --- | --- | --- |
| 1 | `prompt.system.contribute` | A | 回调通道 | 系统提示词片段 | `PROMPT_SYSTEM_CONTRIBUTE` |
| 2 | `prompt.context.contribute` | A | 回调通道 | 每轮动态上下文注入（RAG 片段、环境信息、git status） | `PROMPT_CONTEXT_CONTRIBUTE` |
| 3 | `tool.provide` | B | 回调通道 | 提供工具处理器（路由键 = 工具名） | `TOOL_PROVIDE` |
| 4 | `command.provide` | B | 回调通道 | 提供具名命令处理器 | `COMMAND_PROVIDE` |
| 5 | `session.store.provide` | B | 回调通道 | 会话历史 / 记忆的读写后端 | `SESSION_STORE_PROVIDE` |
| 6 | `approval.channel` | B | 回调通道 | 人工审批「怎么问」 | `APPROVAL_CHANNEL` |
| 7 | `event.subscribe` | C | **事件通道** | 订阅内核通知 | `EVENT_SUBSCRIBE` |
| 8 | `policy.tool.authorize` | E | 回调通道 | **PLAN 模式落点**，只能 deny / escalate | `POLICY_TOOL_AUTHORIZE` |
| 9 | `policy.output.filter` | E | 回调通道 | 工具结果回灌上下文前过滤 | `POLICY_OUTPUT_FILTER` |
| 10 | `prompt.pipeline.intercept` | D | 回调通道 | 提示词组装完成后改写 | `PROMPT_PIPELINE_INTERCEPT` |
| 11 | `llm.request.intercept` | D | 回调通道 | LLM 请求改写（脱敏 / 换模型 / 缓存） | `LLM_REQUEST_INTERCEPT` |

**通道归属规则**：只有 C 走事件通道，其余全部走回调通道。唯一的例外是**不需要返回值**的观察类需求（不属于扩展点，而是 `event.subscribe` 的具体事件类型）。

### 4.2 已删除 / 已排除

| 项 | 处理 | 原因 |
| --- | --- | --- |
| `skill.manifest.contribute` | **删除** | 由 `prompt.system.contribute` + `tool.provide` 表达，属自检通过（A 类够用） |
| `react.loop.hook` | **明确不做** | TODO 用 A + B 即可表达；过早开放会诱发"再早一点的钩子"式蔓延。观察用的**迭代事件**（C）不受影响 |
| `llm.response.intercept` | **明确不做** | 危险：容易破坏工具调用结构 |

---

## 五、能力清单（插件 → 内核，非扩展点）

方向与扩展点相反，不占用通道，由 `PluginContext` 直接暴露：

| # | 能力 | 说明 | 权限常量 |
| --- | --- | --- | --- |
| 1 | `publish(event)` | 发布通知（广播、无返回值） | `EVENT_EMIT` |
| 2 | `configuration()` | 读取插件自己的配置段（来自 `jellyfish.json`，内核已完成双源合并与 `${VAR}` 替换） | — |
| 3 | `tools().list()` | 查询当前已注册的工具清单（供 `/tools`、MCP 同步使用） | — |
| 4 | `session.trigger(sessionId, message)` | **主动向会话投递一条用户消息** | `SESSION_TRIGGER` |

### 5.1 `session.trigger` 的安全约束（实现必带）

这是**唯一一个让插件"写会话"的能力**，实现时必须同时具备：

1. **独立权限**：`SESSION_TRIGGER` 独立于 `EVENT_EMIT`，**不得复用 `publisher()`**（广播与写入语义差太远）；
2. **限流**：按插件 + 会话维度限流，防止刷爆会话；
3. **防自触发循环**：插件触发的消息导致的 turn，不能再触发同一插件（带来源标记，检测到即拒绝并计数）；
4. **目标会话必须已存在**：不允许凭空创建会话（避免绕过会话生命周期与权限）。

典型场景：定时巡检、外部回调（CI 失败）、文件监听、长任务完成后回灌。

---

## 六、回调类型清单（定稿）

回调类型 = 走回调通道的**调用请求对象**，是 `Callback<R>` 的具体子类。

**扩展点 : 回调类型 = 1 : 1..N。** 一个扩展点是一个权限粒度，可以对应多个类型安全粒度的回调类型（如 `session.store.provide` 有 3 个操作，输入输出形状各不相同，无法共用一个类型）。

| # | 回调类型 | 承载扩展点 | 形状 | 路由键 | 请求字段 | 结果类型 | 空表策略 | 插件可注册 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | `ToolCallRequest` | `tool.provide` | B | `toolName` | `toolName`、`arguments` | `ToolCallResult` | REQUIRED | ✅ |
| 2 | `PluginRequest` | `command.provide` | B | `name` | `name`、`payload` | `Object` | REQUIRED | ✅ |
| 3 | `HistoryLoadRequest` | `session.store.provide` | B | `null` | `sessionId` | `List<Message>` | OPTIONAL | ✅ |
| 4 | `MessagePersistRequest` | `session.store.provide` | B | `null` | `sessionId`、`Message` | `Ack` | OPTIONAL | ✅ |
| 5 | `SessionCloseRequest` | `session.store.provide` | B | `null` | `sessionId`、`reason` | `Ack` | OPTIONAL | ✅ |
| 6 | `ApprovalRequest` | `approval.channel` | B | `null` | `agentId`、`toolName`、`arguments`、`reason` | `ApprovalDecision` | REQUIRED | ✅ |
| 7 | `SystemPromptContributionRequest` | `prompt.system.contribute` | A | `null` | `SystemPromptContext` | `List<PromptFragment>` | OPTIONAL | ✅ |
| 8 | `ContextContributionRequest` | `prompt.context.contribute` | A | `null` | `TurnContext` | `List<PromptFragment>` | OPTIONAL | ✅ |
| 9 | `ToolAuthorizeRequest` | `policy.tool.authorize` | E | `null` | `agentId`、`toolName`、`arguments` | `PolicyDecision` | OPTIONAL | ✅ |
| 10 | `OutputFilterRequest` | `policy.output.filter` | E | `null` | `toolName`、`output` | `FilterResult` | OPTIONAL | ✅ |
| 11 | `PromptInterceptRequest` | `prompt.pipeline.intercept` | D | `null` | `PromptDraft` | `PromptDraft` | OPTIONAL | ✅ |
| 12 | `LlmRequestInterceptRequest` | `llm.request.intercept` | D | `null` | `LlmRequest` | `LlmRequest` | OPTIONAL | ✅ |
| 13 | `PermissionCheckRequest` | —（核心独占） | B | `null` | `agentId`、`toolName` | `PermissionDecision` | REQUIRED | ❌ |

**说明：**

- 第 13 项**不对插件开放**（`pluginExtensible=false`）：插件若能注册它就能绕过权限控制。它留在清单里，是为了说明"核心也使用同一条回调通道"，而不是新增机制。
- 第 3~5 项是**同一扩展点的三个操作**。不合并成一个带 routeKey 的类型，是因为三者的**输入输出形状不同**（`List<Message>` / `Message` / `reason`），routeKey 无法表达形状差异。
- **同一个扩展点的不同操作可以有不同的 `execution` 覆盖**：
  - 第 3 项（恢复）**覆盖为 `ISOLATED` + 超时**：B 形状默认 `INLINE`，但慢存储会阻塞会话创建；失败降级为「无历史」。
  - 第 4 项（写入）**保持 `INLINE`**：若用 `ISOLATED` + 超时，超时后丢弃等于丢数据，与 §7.5 的持久化要求直接冲突。

### 6.1 结果类型的单调性设计

三个"判定"类结果类型**在类型层面**保证安全边界：

| 结果类型 | 可用构造 | 语义 |
| --- | --- | --- |
| `PermissionDecision` | `allow(reason)` / `deny(reason)` | 核心权限判定，可放行 |
| `ApprovalDecision` | `approved(by)` / `rejected(by, reason)` | 人工审批结果 |
| `PolicyDecision` | `pass()` / `deny(reason)` / `escalate(reason)` | **没有 `allow()` / `grant()`** |

`PolicyDecision` 不提供任何"放行"构造——插件策略**只能收紧，不能放宽**。这让 §二 第 4 条的"单调 AND 链"由类型系统强制，而不是靠约定。

---

## 七、事件类型清单（定稿）

### 7.1 分组维度

**先按「进程级 / 会话级」分，再按领域分**；消费者只作为标注列（一个事件常有多个消费者，按消费者分组必然重复）。

### 7.2 发布方式

**全部事件一律使用 `publish`（异步）。**

理由：事件的定位就是"观察"。需要同步、需要顺序、需要感知失败的，一律走回调通道（见 `双通道改造方案.md` §四）。`publishSync` 保留为机制能力，但**当前没有任何核心事件使用它**——若将来某个事件需要，必须单独论证，因为它会阻塞发布线程。

### 7.3 事件清单

#### 进程级

| # | 事件 | 关键字段 | 主要订阅方 | 状态 |
| --- | --- | --- | --- | --- |
| 1 | `ApplicationStartedEvent` | 无 | 插件（系统就绪）、指标 | 新增 |
| 2 | `ApplicationStoppingEvent` | `reason` | 插件（释放资源）、指标 | 新增 |
| 3 | `ConfigWarningEvent` | `source`、`message` | 诊断、TUI | 已有 |
| 4 | `PluginStateChangedEvent` | `pluginId`、`state` | 诊断、UI | 已有 |

⚠️ 第 1 项的**发布时机**必须在“全部插件加载完成 **且** 总线 `start()` 之后”，否则订阅方收不到（启动期缓冲只覆盖 `start()` 之前）。第 2 项必须在 `close()` 排空线程池**之前**发出。

#### 通用

| # | 事件 | 关键字段 | 主要订阅方 | 状态 |
| --- | --- | --- | --- | --- |
| 5 | `PluginNotificationEvent` | `source`、`payload` | 插件之间 | 已有 |

#### 会话生命周期

| # | 事件 | 关键字段 | 主要订阅方 | 状态 |
| --- | --- | --- | --- | --- |
| 6 | `SessionCreatedEvent` | `agentId` | 指标、UI | 已有 |
| 7 | `SessionClosedEvent` | `reason`（正常 / 异常 / 超时） | 插件释放资源（MCP 连接等）、指标 | 新增 |
| 8 | `SessionSwitchEvent` | `switchType`（AGENT / MODEL）、切换前后值 | 审计、UI、指标 | 新增 |

#### 消息与轮次

| # | 事件 | 关键字段 | 主要订阅方 | 状态 |
| --- | --- | --- | --- | --- |
| 9 | `MessageAppendedEvent` | `messageId`、`role`、`index`、`length` | 指标、审计、UI | 新增 |
| 10 | `TurnStartedEvent` | `agentId`、`provider`、`model` | 指标、UI | 新增 |
| 11 | `TurnCompletedEvent` | 完成原因（`final` / `error` / `maxIterations`）、耗时、token 用量、工具调用次数 | 指标、记忆提炼、UI | 新增 |

⚠️ 第 9 项**不携带消息正文**：正文体积可能上 MB（`EventBridge` 负载上限 1MB），且工具结果的正文由 `ToolCallCompletedEvent`（执行视角）与会话读取承担。**明确：事件通道不承担持久化**（见 §7.4 约束 1）。

#### 迭代（ReAct 循环）

| # | 事件 | 关键字段 | 主要订阅方 | 状态 |
| --- | --- | --- | --- | --- |
| 12 | `IterationStartedEvent` | `iterationIndex` | UI（"第 N 轮思考"）、指标 | 新增 |
| 13 | `IterationCompletedEvent` | `iterationIndex`、是否产生工具调用、工具调用数、耗时 | 指标（收敛性分析）、审计 | 新增 |

ReAct 的**基本单元**是迭代（思考→行动→观察），不是 turn。缺了这层，指标无法回答"平均几次迭代收敛"，UI 无法显示"第 N 轮思考"。

⚠️ 与已排除的 `react.loop.hook` **不矛盾**：前者是 C 形状（纯观察），后者是 D 形状（可改写流程）。

#### LLM 调用

| # | 事件 | 关键字段 | 主要订阅方 | 状态 |
| --- | --- | --- | --- | --- |
| 14 | `LlmRequestStartedEvent` | `provider`、`model`、消息数、工具数、`attempt` | UI（"思考中"）、超时监控 | 新增 |
| 15 | `LlmResponseCompletedEvent` | `success`、`finishReason`、token 用量、耗时、是否含工具调用 | 指标、成本统计 | 新增 |

⚠️ 两者**都不携带 prompt / response 正文**。`attempt` 字段用于区分重试——**重试会让一个迭代内发生多次 LLM 调用**，这也是 LLM 事件不能与迭代事件合并的原因。

#### 工具调用

| # | 事件 | 关键字段 | 主要订阅方 | 状态 |
| --- | --- | --- | --- | --- |
| 16 | `ToolCallStartedEvent` | `toolCallId`、`toolName` | UI、指标 | 已有 |
| 17 | `ToolCallCompletedEvent` | `toolCallId`、`toolName`、`success`、耗时、错误信息 | 指标、审计、UI | 已有 |

#### 权限与审批

| # | 事件 | 关键字段 | 主要订阅方 | 状态 |
| --- | --- | --- | --- | --- |
| 18 | `PermissionDeniedEvent` | `agentId`、`toolName`、**拒因来源**（核心权限 / 插件策略）、`reason`、`requiresApproval` | 审计、PLAN 模式展示 | 新增 |
| 19 | `ApprovalRequestedEvent` | `agentId`、`toolName`、渠道 | UI（"等待审批中"）、审计 | 新增 |
| 20 | `ApprovalDecidedEvent` | 决定、耗时、决定渠道 | 审计、指标 | 新增 |

⚠️ 第 18 项的 `requiresApproval=true` 表示"被暂停等待审批"，**计入"未执行"**——否则审计会漏掉"没执行"的原因。

### 7.4 硬约束

| # | 约束 |
| --- | --- |
| 1 | **`session.store.provide` 的写路径不走事件通道。** 事件通道（见 `双通道改造方案.md` §四）**不保证到达序**，且**无失败语义**（无返回值、订阅者异常只计数不回传）。持久化需要顺序与失败感知，必须走回调通道的同步调用 |
| 2 | **总线只发「状态变更」，不发「渲染指令」。** 渲染需要严格顺序、高频、低延迟，与事件通道三项特性全部相反。流式片段走点对点回调（如 `LlmStreamListener`），TUI / CLI 渲染走外壳专属的点对点通道 |
| 3 | **异步事件必须自包含**：携带不可变快照，不让订阅方回查会话状态（否则竞态，且会话可能已销毁）。例外见约束 4 |
| 4 | **不重复携带大对象**：`MessageAppendedEvent` 只带 `messageId` + `length`，不带正文 |
| 5 | **每个事件类型都要在 `EventDispatcher` 加一个 `@Subscribe` 方法**（§5.2 的显式登记点）。事件类型表就是**内核的表面积**，新增需回答三问：谁订阅？拿来做什么？为什么现有事件替代不了？ |
| 6 | **一律 `publish`，无 `publishSync`**（§7.2） |

### 7.5 持久化粒度与失败语义（已定）

**每条消息同步落盘。** 不接受「进程崩溃丢最后半轮」，因此消息进入会话消息列表后**立即同步调用** `MessagePersistRequest`。会话关闭时调用 `SessionCloseRequest` 收尾。

**顺序保证**：内核按消息追加顺序同步调用，天然保序——这也是持久化不能走事件通道的原因（§7.4 约束 1）。

**「不丢」的层级**（需明确，直接决定成本）：

| 层级 | 要求 | 代价 |
| --- | --- | --- |
| 1. 进程崩溃不丢 | 写入 OS 缓冲即可（不 `fsync`） | 一次同步调用 + 序列化 |
| 2. 断电 / OS 崩溃不丢 | store 需要 `fsync` / WAL | 每次写盘一次磁盘同步，慢一个数量级 |
| 3. 分布式不丢 | 需要副本 | 复杂度大幅上升 |

**当前按层级 1 设计**（进程崩溃不丢，断电可能丢）。`fsync` 策略由 store 实现自行决定，**内核不强制**——这属于 store 的部署决策，不是内核语义。若要升级到层级 2，只需更换 store 实现，内核零改动。

**失败语义按操作区分**（同一个扩展点内可以不同）：

| 操作 | 失败语义 | 说明 |
| --- | --- | --- |
| `HistoryLoadRequest`（恢复） | **fail-open**，降级为「无历史」 | 恢复失败不该拦住会话创建 |
| `MessagePersistRequest`（写入） | **fail-closed**，中止当前 turn 并上抛 | 既然不接受丢数据，写失败就必须让调用方知道 |
| `SessionCloseRequest`（收尾） | **fail-open**，记录并继续关闭 | 关闭阶段的失败不该阻塞资源释放 |

⚠️ **「空表」与「失败」是两件事**：没有注册 store = 不持久化（正常）；注册了 store 但写入失败 = 中止 turn。

⚠️ **写入的 `execution` 必须是 `INLINE`**（不能用 `ISOLATED` + 超时）：超时后丢弃等于丢数据，与本节要求直接冲突。写入耗时由 store 自己优化。

### 7.6 事件总量评估

一个含 10 次工具调用的 turn 约产生：1 `TurnStarted` + 3 `IterationStarted` + 3 `IterationCompleted` + 4 `LlmRequestStarted` + 4 `LlmResponseCompleted` + 10 `ToolCallStarted` + 10 `ToolCallCompleted` + 若干 `MessageAppended` + 1 `TurnCompleted` ≈ **35+ 事件**。

默认有界队列 1024。高并发多会话时可能触发拒绝降级（降级为 inline 执行，会阻塞发布线程）。**上线前需按实际会话并发量复核队列容量参数。**

---

## 八、扩展点契约卡片格式

每个扩展点按以下格式定义；定完即可反推 `PluginContext` 接口：

| 字段 | 说明 |
| --- | --- |
| 扩展点 ID | 稳定标识，用于 `plugin.properties` 声明与权限校验 |
| 形状 | A / B / C / D / E |
| 通道 | 回调通道 / 事件通道 |
| 承载的回调类型 | 一个或多个（C 无） |
| 生效时机 | 会话创建 / 每轮 turn 开始 / 提示词组装后 / LLM 调用前 / 工具调用前 / 工具结果回灌前 / turn 结束 / 会话关闭 |
| 入参 | 精确类型 |
| 出参 | 精确类型 |
| 可否改写结果 | 是 / 否 |
| 可否短路 | 是 / 否 |
| 失败语义 | fail-open 降级 / fail-closed |
| 是否有序 | 有序则给出 `order` 语义与默认值 |
| 参数覆盖 | 相对形状默认值的覆盖项 |
| 权限常量 | 权限标识 |
| 热部署行为 | 下一次组装生效 / 需重建会话 |

---

## 九、A / B 拆分：记忆与上下文注入

### 9.1 问题

「记忆」横跨两条正交路径，被一个词绑在了一起：

- **写路径 + 存储后端**：有资源、有生命周期、会话级唯一、需要「必须成功否则会话不完整」的语义 → **只能是 B**
- **读路径 + 注入本轮上下文**：无状态、多来源、需要全局预算仲裁 → **只能是 A**

### 9.2 决定性事实

`Session` **已经负责按当前模型的 `contextLength` 裁剪上下文**（见 `Jellyfish技术方案.md` §4.3）。裁剪既然在内核手里，预算仲裁就只能在内核手里——否则插件塞的内容要么撑爆窗口，要么被裁得只剩半句，而插件自己无从知晓。**这一条判定"注入上下文"必须走 A。**

反过来，A **没有写路径、没有生命周期**：记忆的提炼、持久化、索引、去重，A 一概做不了。

### 9.3 拆分方案

| 扩展点 | 形状 | 职责边界 |
| --- | --- | --- |
| `prompt.context.contribute` | A | **所有**"往本轮上下文塞内容"的唯一入口。插件无状态，只回答"我贡献哪一段" |
| `session.store.provide` | B | 会话历史 / 记忆的**读写后端**：持久化、恢复、检索索引、连接与生命周期 |

**记忆插件通常两个都实现**：store 负责存与检索，contributor 负责把检索结果注入本轮。

装配顺序固定为：`系统提示词 + 所有 A 类贡献 + 历史消息`。**B 不参与 prompt 组装**，它想进 prompt 也必须经由 A。

### 9.4 两种"不拆"的做法都有硬伤

- **只用 A 做记忆**：插件拿不到"别人塞了多少"，要么撑爆上下文，要么自己的内容被裁掉还浑然不知；且写路径无处安放。
- **只用 B 做记忆**：RAG、环境信息全都得硬塞进那个后端，长期记忆与检索耦合；且 B 是独占的，两个记忆源直接打架。

### 9.5 A / B 优劣对照

| 维度 | A 贡献 | B 提供 |
| --- | --- | --- |
| 数量语义 | 0..N，天然可组合 | 恰好 1，独占 |
| 插件是否有状态 | 无状态，纯函数 `ctx → 片段` | 有状态：连接池、索引、预热、缓存 |
| 写路径 | **没有**（只有读） | 天然有（同一后端读写闭环） |
| 失败影响 | 局部降级 | 整块记忆消失 |
| 权限粒度 | 细：只准注入一段 | 粗：独占整个会话的记忆 |
| 热部署 | 影响小，符合快照策略 | 中途换后端 = 记忆语义突变，风险高 |
| 预算仲裁 | 内核可全局仲裁 | 插件只能自估，不知道别的消费者要多少 |
| 超窗风险 | 不会（内核裁） | 会 |
| 开发成本 | 低，写个纯函数 | 高，要管生命周期与线程安全 |

### 9.6 与形状绑定的硬约束

1. **A 类必须带「剩余预算」入参**：`PromptContext` 要告知贡献者"还剩多少 token / 字符"。不给，它只能瞎塞。
2. **A 类必须有 per-handler 超时 + 降级**：prompt 组装是同步的，一个做 3 秒向量检索的贡献者会拖垮所有会话。超时即丢弃该片段（由回调通道的 `ISOLATED` 模式提供）。
3. **B 类必须按操作明确 fail 语义**：`session.store.provide` 的**恢复**是 fail-open（降级为无历史），**写入**是 fail-closed（中止 turn），**收尾**是 fail-open。详见 §7.5。
4. **E 类 fail-closed，其余 fail-open**（§二 第 5 条）。

---

## 十、明确不做的事

| 项 | 原因 |
| --- | --- |
| `react.loop.hook`（ReAct 循环钩子） | TODO 用 A + B 即可表达；过早开放会诱发"再早一点的钩子"式蔓延。观察用的迭代**事件**（C）不在排除之列 |
| `llm.response.intercept`（改写模型回复） | 危险：容易破坏工具调用结构 |
| `skill.manifest.contribute`（独立扩展点） | 由 `prompt.system.contribute` + `tool.provide` 表达 |
| 按功能切分 SPI（`ToolPlugin` / `MemoryPlugin` / `CrossPlugin`） | 功能发散，每加一类能力都要改 `jellyfish-api` |
| 插件自行读取配置文件 | 破坏隔离，无法纳入双源合并 |
| 插件注册 `PermissionCheckRequest` 处理器 | 会绕过权限控制（`pluginExtensible=false`） |
| 插件间直接通信 | 走总线即可，且应尽量避免耦合 |

> **除上表以外，§四 的 11 个扩展点、§五 的 4 项能力、§六 的 13 个回调类型、§七 的 20 个事件类型全部实现。**

---

## 十一、后续工作顺序

1. **逐条展开扩展点契约卡片**（§八 格式），分四批：
   - 第一批：A 类两条（`prompt.system.contribute` / `prompt.context.contribute`）
   - 第二批：B 类四条（`tool.provide` / `command.provide` / `session.store.provide` / `approval.channel`），顺带定出 `ToolDescriptor`
   - 第三批：E 类两条（`policy.tool.authorize` / `policy.output.filter`）
   - 第四批：D 类两条（`prompt.pipeline.intercept` / `llm.request.intercept`）
2. **反推 `PluginContext` 接口**（按扩展点分组暴露注册入口，按能力分组暴露能力）
3. **实现 `双通道改造方案.md`**（回调通道泛化 → `ISOLATED` 执行器 → 扩展点定义注册表）
4. **定义 `ToolDescriptor` 与插件配置注入**
5. **事件类型逐个实现**（含 `EventDispatcher` 登记点）
6. **官方插件逐个落地**，以 **MCP 作为扩展点表达力的试金石**
