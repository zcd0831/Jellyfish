# Jellyfish 双通道改造方案

> 本文定义 Jellyfish 内核与插件之间的**机制层**：把现有的「同步命令通道 + 异步通知通道」改造为「**回调通道 + 事件通道**」。
> 语义层（形状预设）与实例层（具体扩展点）见 `扩展点设计方案.md`。
>
> 关联文档：`AGENTS.md`、`Jellyfish技术方案.md`、`跨语言插件方案.md`、`扩展点设计方案.md`

---

## 一、定位与背景

### 1.1 一句话定位

**回调通道负责「有返回值的调用」，事件通道负责「无返回值的广播」。** 两者是同一条总线门面下的两条通道，共用同一套注册表生命周期（owner 回收、诊断快照、注册期校验）。

### 1.2 为什么要改造

改造前，内核定义了两条通道：

| 通道 | 语义 | 处理器数 | 返回值 |
| --- | --- | --- | --- |
| 同步命令通道 | 请求-响应 | **恰好 1** | 有（单个） |
| 异步通知通道 | 广播 | 0..N | **无** |

但扩展点体系需要第三种语义——**0..N 且要返回值**：

| 形状 | 需求 | 现有两条通道能否承载 |
| --- | --- | --- |
| **A 贡献** | 收集所有插件贡献的片段 | ❌ 命令通道只能有 1 个处理器；事件通道拿不到返回值 |
| **D 拦截** | 有序链式改写，传递中间结果 | ❌ 同上 |
| **E 策略** | 逐个询问，任一 deny 即拒 | ❌ 同上 |

硬塞进命令通道的两条路都不好：

1. 让内核枚举路由键、**逐个 dispatch N 次** —— 丢掉"原子快照"语义，且内核要自己拼装；
2. 允许一个命令类型注册多个处理器 —— 破坏 `CommandRegistry` 已写死的"`AMBIGUOUS_HANDLER` 即错误"不变量。

### 1.3 改造方向

**不新增第三条通道，而是把命令通道泛化为回调通道。**

关键观察：「恰好 1 + 有返回值」本来就是「0..N + 有返回值」的**退化特例**。真正区分两条通道的不是"同步还是异步"，而是**有没有返回值**。

```
改造前：命令通道（恰好 1 + 有返回值） + 通知通道（0..N + 无返回值）
改造后：回调通道（0..N + 有返回值）   + 事件通道（0..N + 无返回值）
                    └── 命令是 unique=true 时的特例
```

---

## 二、设计原则

### 2.1 三层分层

```
机制层   回调通道 / 事件通道                    ← 只区分「有无返回值」
语义层   A 贡献 / B 提供 / D 拦截 / E 策略      ← 每个形状一组固定的参数预设
实例层   prompt.system.contribute / tool.provide ← 声明自己属于哪个形状
```

**插件作者只看到实例层。** 插件调用 `ctx.<扩展点>().register(...)`，永远不写 `unique=true` 这类参数——形状的参数是内核的实现细节，向插件暴露只会导致每个插件一套语义。

### 2.2 通道只提供机制，组合规则属于调用方

回调通道只保证两件事：**有序调用** + **收集返回值**。聚合、链式还是 AND，由内核在每个扩展点的调用处决定：

| 形状 | 组合规则 | 说明 |
| --- | --- | --- |
| A 贡献 | 聚合 | 收集全部片段，按剩余预算裁剪后合并 |
| D 拦截 | 链式 | 每个拦截器拿到前一个的输出 |
| E 策略 | AND | 逐个询问，任一 deny 即拒，异常按 fail-closed 处理 |

> 这条必须显式写明，否则后续一定有人以为"注册到回调通道就自动聚合了"。

### 2.3 唯一一个向插件开放的参数是 `order`

| 参数 | 谁决定 | 理由 |
| --- | --- | --- |
| `unique` / `ordered` / `emptyPolicy` / 结果数 | **扩展点定义** | 属于调用语义，插件无判断依据 |
| **`order`** | **插件** | 只有插件自己知道"我的上下文比别人的更重要" |
| `override`（既有） | 插件 | 冲突解决属于插件的业务决策 |

---

## 三、回调通道语义

### 3.1 核心语义

> 内核按某个**扩展点**发起一次调用，通道把该调用分发给此扩展点的**全部**处理器，**有序、按序**收集它们的返回值。

### 3.2 参数轴

| 轴 | 取值 | 含义 |
| --- | --- | --- |
| `unique` | `true` / `false` | 是否只允许一个处理器。`true` 时重复注册按 `override` 规则处理，冲突报错 |
| `ordered` | `true` / `false` | 是否有顺序语义。`true` 时处理器按 `order` 升序调用（默认 order = 0，同序按注册顺序） |
| `emptyPolicy` | `REQUIRED` / `OPTIONAL` | 无处理器时：硬失败（`NO_HANDLER`）/ 视为"无贡献"继续 |
| `resultArity` | `ONE` / `MANY` | 调用方要单个结果还是全部结果 |
| `pluginExtensible` | `true` / `false` | 是否允许插件注册（现 `@PluginExtensible`，**与 `unique` 正交**） |
| `execution` | `INLINE` / `ISOLATED` | 见 §3.5 |
| `failurePolicy` | `FAIL_OPEN` / `FAIL_CLOSED` | 单个处理器失败时丢弃该结果，还是让整次调用失败（形状默认见 §5） |

**`pluginExtensible` 与 `unique` 必须分开看**：前者是安全边界（谁能注册），后者是调用语义（能注册几个）。例如 `PermissionCheckRequest` 是 `unique=true` **且** `pluginExtensible=false`——插件既不能注册，也不能覆盖。

### 3.3 结果收集

- `resultArity = ONE`：返回 `R`，等价于现有 `dispatch` 行为，保留三道防线；
- `resultArity = MANY`：返回 `List<R>`，**按处理器调用顺序排列**（不是完成顺序，因为 §3.5 默认串行）；
- 结果类型仍由扩展点声明，运行时校验每个返回值，不匹配即抛出。

### 3.4 空表行为

| 取值 | 适用形状 | 说明 |
| --- | --- | --- |
| `REQUIRED` | B 提供 | 无处理器 = `NO_HANDLER` **硬失败**（调用方依赖结果） |
| `OPTIONAL` | A / D / E | 无处理器 = 无贡献 / 直通 / 不额外拒绝 |

### 3.5 执行模式与超时

现有命令通道是**调用线程内联执行**，处理器无法被外部打断，超时只能由处理器自律（已列为已知边界）。

这个限制对 A / D / E 是**不可接受**的：prompt 组装是同步的，一个做 3 秒向量检索的贡献者会拖垮**所有**会话。因此回调通道需要第二种执行模式：

| 模式 | 行为 | 超时 | 适用 |
| --- | --- | --- | --- |
| `INLINE` | 调用线程内联执行（现状） | 处理器自律，`deadlineNanos` 仅作参考 | B 提供 |
| `ISOLATED` | 提交到专用线程池，**按序逐个调用**，每个用 `Future.get(perHandlerTimeout)` 等待 | **内核可强制超时**，超时 `cancel(true)` 中断并丢弃该处理器的结果 | A 贡献 / D 拦截 / E 策略 |

`ISOLATED` 的**顺序保证方式**：不是并发调用后排序，而是**在单个工作线程上按 order 依次调用**，逐个施加超时。这样既保序，又能对慢处理器施加超时。

`ISOLATED` 的代价（需接受）：

1. 处理器不在调用线程上运行，**不能依赖 ThreadLocal**（现有设计已把 `sessionId` 放在对象上，天然兼容）；
2. `Future.cancel(true)` 是**尽力而为**的中断——遇到不可中断的阻塞代码仍会挂住工作线程，需要工作线程池有独立的有界队列与拒绝策略兜底；
3. 每个处理器一次线程切换开销。

超时后的失败语义由**回调类型**声明，形状给默认：A / D **fail-open**（丢弃该片段/该拦截器），E **fail-closed**（视为 deny）。

也存在**不施加超时**的情形：`MessagePersistRequest`（会话消息写入）必须用 `INLINE` 并等待完成——超时后丢弃等于丢数据，与持久化要求直接冲突。因此 `execution` 与失败语义都是**扩展点（乃至同一扩展点的不同操作）可覆盖**的参数，不是通道的硬属性。

---

## 四、双通道对照

| 维度 | 回调通道 | 事件通道 |
| --- | --- | --- |
| 返回值 | **有** | **无** |
| 处理器数 | 0..N（`unique=true` 时恰好 1） | 0..N |
| 同步性 | 同步（调用方等结果） | 异步（`publish`）/ 同步（`publishSync`） |
| 顺序 | 有序形状保证按 order 逐序调用 | **不保证**（§8.3） |
| 失败语义 | 可回传调用方（`ONE`）/ 逐处理器降级（`MANY`） | 无，仅计数与记日志 |
| 空表 | `REQUIRED` 硬失败 / `OPTIONAL` 继续 | 正常 |
| 典型场景 | 工具、命令、会话存储、审批、提示词贡献、策略 | 指标、审计、UI 状态变更 |

**唯一的通道分界是"有没有返回值"。** 需要顺序、需要结果、需要感知失败的一律走回调通道；纯记录、可容忍乱序与偶发丢弃的走事件通道。

---

## 五、形状预设（语义层）

| 形状 | unique | ordered | emptyPolicy | resultArity | execution | failurePolicy | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| **A 贡献** | false | true | OPTIONAL | MANY | ISOLATED | FAIL_OPEN | 插件可声明 `order` |
| **B 提供** | **true** | false | **REQUIRED** | ONE | INLINE | FAIL_CLOSED | 按路由键分发，不存在先后 |
| **D 拦截** | false | true | OPTIONAL | MANY | ISOLATED | FAIL_OPEN | 插件可声明 `order` |
| **E 策略** | false | true | OPTIONAL | MANY | ISOLATED | FAIL_CLOSED | 顺序仅用于确定性，语义上与顺序无关 |

> **已确认**：`order` **只对有序形状开放**（A / D / E）。B 提供按 key 路由，不存在"谁先谁后"。

**形状给默认值，扩展点可覆盖。** 参数预设是形状级的缺省，不是硬约束——扩展点定义有权覆盖任何一项。已知需要覆盖的例子：

- `session.store.provide`（B 形状）的**历史恢复**需要 `ISOLATED` + 超时，否则一个慢存储会阻塞会话创建；B 的默认 `INLINE` 对它不成立。覆盖后失败降级为「无历史」。
- 同一个扩展点的**不同操作可以有不同覆盖**：上例中**恢复**用 `ISOLATED`，而**消息写入**保持 `INLINE`（写入不能超时丢弃）。

覆盖必须**显式声明在扩展点定义里**（见 `双通道改造方案.md` §八 第 9 项），不允许在调用点临时改变。

---

## 六、类型契约改造

### 6.1 改造清单

| 现有 | 改造 | 说明 |
| --- | --- | --- |
| `Command<R>` | 语义扩展为"回调通道的调用请求" | 字段（`callbackId`/`sessionId`/`deadlineNanos`）保留 |
| `CommandHandler<C,R>` | 语义扩展 | 仍是类型化的正向契约：`return 结果 / throw 异常` |
| `CommandRegistrar` | 语义扩展，新增 `order` 入口 | 插件侧只按扩展点调用 |
| `CommandRegistry` | **唯一性硬编码 → 由扩展点定义驱动** | `resolve()` 分叉为 `resolveUnique()` / `resolveAll()` |
| `CommandReplies` | 单结果应答槽 → 支持**收集多结果** | 按 `callbackId` 收集列表 |
| `CommandDispatcher` | `invoke()` 分叉为 `invokeOne()` / `invokeAll()` | **per-type `@Subscribe` 登记点不变** |
| `CommandException.Code` | `NO_HANDLER` / `AMBIGUOUS_HANDLER` 降级为"该扩展点策略的表现"，不再是全局错误码 | 语义随扩展点而变 |
| `JellyfishEventBus` | `dispatch()` 分叉；**门面职责描述需改写** | 见 §6.3 |
| `PluginContextImpl` | 新增回调注册入口 | 并回传扩展点定义 |
| `EventRegistry` / `EventDispatcher` | **不变** | 事件通道零改动 |

### 6.2 保持不变的部分

- **`@PluginExtensible` 白名单机制**：仍按类型级注解做注册期越权拦截；
- **owner 批量回收**：`unregisterAll(owner)` 对回调与事件一视同仁，插件卸载即回收；
- **`RegistrySnapshot` 诊断快照**：新增回调条目后仍能回答"这个扩展点谁提供 / 这个插件注册了什么"；
- **per-type `@Subscribe` 显式登记点**：新增扩展点类型时仍需在 dispatcher 加一个方法——这是**有意保留**的登记点（可 review、可加约定、可加指标）；
- **三段式防线**：`NO_HANDLER` 判定、处理器异常回传、`NO_RESPONSE` 兜底，在 `unique` 形状下原样保留。

### 6.3 一个连带后果：门面名字开始误导

加上泛化后的回调通道，`JellyfishEventBus` 不再只是"事件总线"，它是**内核与插件之间的唯一交互枢纽**（三张表：回调、事件、加上既有的事件注册表）。

本方案**不改名**（改名会波及全部文档与测试），但要求：

1. 类的 javadoc 改为"内核与插件交互枢纽"，不再自称"事件总线"；
2. 后续若要改名（如 `PluginBroker` / `InteractionHub`），单独决策、单独提交。

### 6.4 命名决策（已确认：本轮重命名）

`Command` 语义上确实变宽了（A 贡献不像“命令”），且当前 `infra/plugin` 仍为空目录、**没有任何插件依赖现有命名**，因此本轮统一重命名为 `Callback*` / `XxxRequest`：

| 现有 | 新名 |
| --- | --- |
| `Command<R>` | `Callback<R>` |
| `CommandHandler<C, R>` | `CallbackHandler<C, R>` |
| `CommandRegistrar` | 拆分为 `ToolRegistrar`（`tool.provide`）+ `CommandRegistrar`（`command.provide`），插件侧按扩展点注册（见 §十 阶段 4） |
| `CommandRegistry` | `CallbackRegistry` |
| `CommandKey` | `CallbackKey` |
| `CommandRegistration` | `CallbackRegistration` |
| `CommandException` | `CallbackException` |
| `CommandReplies` | `CallbackReplies` |
| `CommandDispatcher` | `CallbackDispatcher` |
| `ToolCallCommand` | `ToolCallRequest` |
| `PermissionCheckCommand` | `PermissionCheckRequest` |
| `PluginCommand` | `PluginRequest` |
| `PluginCommandHandler` | `PluginRequestHandler` |

包路径同步调整：`zcd.jellyfish.api.event.command` → `zcd.jellyfish.api.event.callback`，`zcd.jellyfish.infra.event.command` → `zcd.jellyfish.infra.event.callback`。

保持不变：`PluginExtensible`、`PermissionDecision`、`ToolCallResult`、`RegisterOptions`、`Subscription`。

> §6.1 / §八 中出现的是**改造前**的名字，重命名映射以本表为准。

---

## 七、调用流程

### 7.1 唯一型（`resultArity = ONE`，即原命令）

```java
// 调用方
R result = callbackChannel.invoke(request);     // 内部：resolveUnique → 不存在则 NO_HANDLER
```

保持现有三道防线与 `CommandReplies` 行为不变。

### 7.2 收集型（`resultArity = MANY`）

```java
// 调用方（以 A 类贡献为例）
List<PromptFragment> fragments = callbackChannel.invokeAll(request);
// 内部：resolveAll → 按 order 逐个调用（ISOLATED，逐个超时）→ 收集成功结果 → 丢弃超时/异常项
```

**组合不在这里发生**。调用方拿到 `List` 后自己决定聚合、链式还是 AND。

### 7.3 事件的对应关系

事件通道不变：`publish(event)` 异步广播，`publishSync(event)` 同步广播。两者都无返回值、无失败回传，因此**不能**作为任何"需要顺序或需要感知失败"的用途。

---

## 八、重构清单（按文件）

| # | 文件 | 改动 |
| --- | --- | --- |
| 1 | `infra/event/command/CommandRegistry.java` | `resolve()` 分叉；唯一性改由扩展点定义驱动；`AMBIGUOUS_HANDLER` 仅在 `unique=true` 时作为错误 |
| 2 | `infra/event/CommandReplies.java` | 新增多结果收集能力（按 `callbackId` 收集 `List`） |
| 3 | `infra/event/CommandDispatcher.java` | `invoke()` 分叉为 `invokeOne()` / `invokeAll()`；**保留 per-type `@Subscribe` 方法** |
| 4 | `infra/event/JellyfishEventBus.java` | `dispatch()` 分叉为 `invoke()` / `invokeAll()`；javadoc 改为交互枢纽 |
| 5 | `api/event/command/CommandRegistrar.java` | 新增 `order` 声明入口；按扩展点暴露注册方法 |
| 6 | `api/event/command/CommandException.java` | `Code` 语义调整（`NO_HANDLER` 仅在 `REQUIRED` 下成立） |
| 7 | `api/event/command/PluginExtensible.java` | 保持；确认与 `unique` 正交后 javadoc 补充说明 |
| 8 | `infra/event/PluginContextImpl.java` | 新增回调注册入口，绑定扩展点定义 |
| 9 | **新增** 扩展点定义注册表 | 承载形状预设（`unique` / `ordered` / `emptyPolicy` / `resultArity` / `execution`） |
| 10 | **新增** `ISOLATED` 执行器 | 有序逐调 + 逐处理器超时 + 中断；独立有界线程池与拒绝策略 |
| 11 | `infra/event/notification/**` | **不变** |

---

## 九、兼容性与影响面

### 9.1 既有实现与测试

- `CommandRegistry` 相关单测需按新语义调整（唯一性由扩展点定义驱动，不再按类型硬编码）；
- `PluginContextImplTest` 需覆盖新的注册入口；
- `dispatch()` 现有调用点（若有）保持行为不变——`unique` 形状是默认路径。

### 9.2 需要同步更新的文档

| 文档 | 需更新处 | 状态 |
| --- | --- | --- |
| `Jellyfish技术方案.md` | §1.2 / §1.4 / §2.1 / §2.2 中“双通道：同步命令 + 异步通知”的表述 | ✅ 已对齐 |
| `AGENTS.md` | §整体架构图中 `EventBus` 的描述、`infra/event` 目录说明 | ✅ 已对齐 |
| `跨语言插件方案.md` | 脚本侧术语与回调类型名（`ToolCallRequest` / `PluginRequest`）、A/D/E 在脚本侧的注册形式 | ✅ 已对齐 |
| ~~`事件总线技术方案.md`~~ | **该文档已废弃删除**，相关机制全部以本文档为准 | — |

---

## 十、落地阶段

| 阶段 | 内容 | 状态 |
| --- | --- | --- |
| 1 | 定义扩展点定义注册表（形状预设），先把 A / B / D / E 的参数冻结 | ✅ 已完成 |
| 2 | 改造 `CallbackRegistry` + `CallbackReplies` + `CallbackDispatcher`，保持 `unique` 形状行为不变（回归测试护航） | ✅ 已完成（含 `Command*` → `Callback*` 全量重命名） |
| 3 | 新增 `ISOLATED` 执行器与逐处理器超时 | ✅ 已完成 |
| 4 | `PluginContext` 按扩展点暴露注册入口 | ✅ 已完成（`tools()` / `commands()`；后续扩展点随契约落地逐个增加） |
| 5 | 同步更新既有文档 | ✅ 已完成 |
| 6 | 以 MCP 插件作为扩展点表达力的试金石（见 `扩展点设计方案.md`） | 待开始 |

---

## 十一、决策记录

| # | 决策 | 理由 |
| --- | --- | --- |
| **D1** | 不新增第三条通道，把命令通道泛化为回调通道 | 「恰好 1 + 有返回值」是「0..N + 有返回值」的退化特例，通道分界本质是"有没有返回值" |
| **D2** | 通道参数（`unique` / `ordered` / `emptyPolicy` / 结果数）由扩展点固定，不向插件暴露 | 向插件暴露会导致每个插件一套语义 |
| **D3** | `order` 是唯一向插件开放的参数，且只对有序形状开放 | 只有插件知道自己相对他人的优先级；B 按 key 路由，无先后可言 |
| **D4** | 组合规则（聚合 / 链式 / AND）属于调用方，不属于通道 | 通道只保证"有序调用 + 收集返回值" |
| **D5** | A / D / E 使用 `ISOLATED` 执行模式，内核可强制超时 | prompt 组装同步，慢贡献者会拖垮所有会话；INLINE 无法从外部打断 |
| **D6** | `pluginExtensible` 与 `unique` 视为正交两轴 | "谁能注册"是安全边界，"能注册几个"是调用语义 |
| **D7** | 门面暂不改名，但 javadoc 职责改为"交互枢纽" | 改名波及全部文档与测试，收益仅是命名观感 |
| **D8** | 类型名统一重命名为 `Callback*` / `XxxRequest`，包路径改为 `event.callback` | 窗口期最优：`infra/plugin` 仍为空，没有任何插件依赖现有命名；语义上命令只是回调的退化特例 |

---

## 十二、待确认项

| # | 待确认 | 建议 |
| --- | --- | --- |
| Q1 | ~~`Command*` 系列是否本轮重命名~~ | **已确认：本轮重命名**，映射见 §6.4 / D8 |
| Q2 | ~~`ISOLATED` 执行器的线程池参数（大小、队列、拒绝策略）~~ | **已确认并落地**：核心 4 / 最大 32 / 队列 256 / 空闲回收 60s，守护线程；拒绝即降级（丢弃该处理器结果并计数，并抛 `RejectedExecutionException` 让调用方立即失败，而不是等到超时） |
| Q3 | ~~`ISOLATED` 下是否需要 per-handler 超时与整体超时两级~~ | **已确认并落地**：只做 per-handler 超时，默认 2000ms |
| Q4 | ~~扩展点定义注册表的落地形态（注解 / 代码注册 / 配置）~~ | **已确认并落地**：`@ExtensionPoint` 注解声明 + `ExtensionPointRegistry` 启动期校验（`withBuiltIns()` 登记内置定义，未登记类型在注册/解析时 fail-fast） |
